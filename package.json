{
  "name": "automated-course-content-generator",
  "version": "1.0.0",
  "description": "Streamlit App",
  "main": "app.py",
  "scripts": {
    "start": "pip install -r requirements.txt && python -m streamlit run app.py --server.port 3000 --server.address 0.0.0.0",
    "dev": "pip install -r requirements.txt && python -m streamlit run app.py --server.port 3000 --server.address 0.0.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}